# audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
scipy>=1.11.0

# speech recognition
faster-whisper>=1.0.0
openai-whisper>=20231117  # WHISPER_ENGINE="whisper" 用（フォールバック）
vosk>=0.3.45              # WHISPER_ENGINE="vosk" 用。完全無料・オフライン・軽量。モデルは別途手動ダウンロードが必要
silero-vad>=6.0           # VAD（音声区間検出）用。完全無料・オフライン。モデルはパッケージに同梱済みでダウンロード不要
noisereduce>=3.0.0        # マイク感度が低いため増幅前にノイズ除去する

# speaker diarization
resemblyzer>=0.1.3        # DIARIZER_MODE="resemblyzer" 用（フォールバック）
pyannote.audio>=3.1.0     # DIARIZER_MODE="pyannote" 用。利用にはHuggingFaceの利用規約同意とトークンが必要
huggingface_hub<1.0       # pyannote.audio 4.0.7が1.x系のAPI変更に非対応でgatedモデル取得に失敗するため固定
omegaconf>=2.3.0          # pyannote.audioが暗黙に必要とするが自動インストールされないため明記

# display / communication
websockets>=12.0

# direction detection (step 5: reSpeaker XVF3800 mic array)
pyusb>=1.2.1
libusb-package>=1.0.26; sys_platform == 'win32'

# sound event recognition (HoloSound論文2.2節の再現: 音の種類を19種に分類)
panns-inference>=0.1.1    # AudioSet 527種で学習済みのCNN14。初回実行時にモデル(約320MB)を~/panns_data/へ自動取得
torchlibrosa>=0.1.0       # panns-inferenceがログメル特徴の計算に使う
